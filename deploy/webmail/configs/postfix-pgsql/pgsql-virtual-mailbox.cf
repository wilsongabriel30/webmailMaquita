# Postfix → PostgreSQL: buzones virtuales. __DB_PASS__ se reemplaza al instalar.
hosts = localhost
user = mailserver
password = __DB_PASS__
dbname = maildb
query = SELECT maildir FROM mailbox WHERE username='%s' AND active = true
