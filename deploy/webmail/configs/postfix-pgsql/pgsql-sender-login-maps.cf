# Postfix → PostgreSQL: mapa remitente→login (evita suplantación). __DB_PASS__ se reemplaza al instalar.
hosts = localhost
user = mailserver
password = __DB_PASS__
dbname = maildb
query = SELECT username FROM mailbox WHERE username='%s' AND active = true
