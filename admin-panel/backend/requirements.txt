# Versiones fijadas el 2026-09-04 a partir de lo que realmente corre en produccion
# (panel de administracion (VM 130)). Fijar no actualiza nada: deja por escrito lo que ya estaba instalado,
# para que una instalacion nueva no traiga versiones distintas sin querer. [C2]
fastapi==0.136.3
uvicorn[standard]==0.49.0
asyncpg==0.31.0
bcrypt==5.0.0
httpx==0.28.1
PyJWT==2.13.0
pydantic==2.13.4
aioimaplib==2.0.1
python-multipart==0.0.30
redis==8.0.0
gunicorn==26.0.0
pyotp==2.10.0
qrcode==8.2
