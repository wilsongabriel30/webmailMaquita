# Cambios 2026-06-04 — Para aplicar en otra instalación

Resumen de los arreglos del día. Hacé `git pull` y seguí las notas de
"Aplicar" (lo que no es solo código: nginx, .env, tablas, reinicios).

## 1. git pull
Todo el código (frontend, backend, panel, instalador) está en `main`.

## 2. Vista previa de Office (OnlyOffice) — IMPORTANTE
**Qué se arregló:** el preview salía en blanco. Causas: (a) el frontend pasaba
el elemento DOM en vez del `id` a `DocsAPI.DocEditor`; (b) faltaban rutas nginx;
(c) faltaba `proxy_http_version 1.1` en `/onlyoffice/`. Ahora usa `type=embedded`.

**Aplicar en nginx:** la plantilla `deploy/webmail/nginx/webmail.conf` ya trae
las 4 rutas (`/onlyoffice/`, `/cache/files/`, `/doc/{id}/c/`, `/coauthoring/`).
Si ya tenías nginx instalado, **agregá esas 4 rutas a tu vhost** y cambiá
`127.0.0.1:8080` por la **IP:puerto de tu OnlyOffice Document Server**.
Sin esas rutas (especialmente el WebSocket `/doc/.../c/`) el editor queda en
blanco. `proxy_http_version 1.1` es obligatorio.

**Aplicar en .env / panel:** poné `ONLYOFFICE_SECRET` (= el JWT secret de tu
Document Server) y opcional `ONLYOFFICE_URL`. O configuralo en el panel
:8443 → **OnlyOffice / Nube** (tabla `office_config`, la crea el panel solo).

## 3. Firma del compositor
- El botón Firma **despliega un menú con las firmas creadas** (`/settings/signatures`)
  para elegir; reemplaza, no duplica. Menú con portal (se ve por encima).
- Ya **no se inserta la firma dentro del editor** (el Toolbar lo hacía); va por
  evento a ComposePanel y se muestra editable abajo.
- Borradores se guardan **sin** la firma incrustada (evita doble firma al enviar).
- Solo código → con `git pull` + rebuild del frontend alcanza.

## 4. Otros fixes de UI (solo código + rebuild frontend)
- **Pestañas de borradores** muestran el asunto real (sync al store).
- **Recordatorios de contacto** (cumpleaños): campo `type=date`, no pide hora,
  no rompe la página.
- **"Nombre para mostrar"** en contactos se autocalcula (nombre + apellido).
- **Fijar mensaje (chincheta)**: implementado de verdad (`lib/pins.ts`,
  localStorage; los fijados van arriba con ícono).

## 5. Dictado por voz (Whisper) — configurable
**Qué hace:** graba audio en el navegador → `POST /api/mail/transcribe` (backend)
→ proxea a un servidor **Whisper** → inserta el texto.

**Aplicar:**
- El backend lee la tabla `voice_config` (la crea el panel) o el `.env`
  (`WHISPER_URL`, `WHISPER_API_KEY`, `WHISPER_LANGUAGE`). Ver `.env.example`.
- Configurable desde el panel :8443 → **Dictado por voz** (con guía).
- Tu servidor Whisper debe exponer `GET /health` y `POST /api/transcribe`
  (campo `audio`, cabecera `X-API-Key`) y devolver JSON con `full_text`.
- Reiniciá el backend del webmail y el del panel tras `git pull`.

## 6. Panel :8443 — secciones nuevas
- **OnlyOffice / Nube** y **Dictado por voz** (parametrización + guía).
- Las tablas (`office_config`, `voice_config`) se crean solas al arrancar el
  backend del panel. Reiniciá `maquita-admin` (o tu servicio del panel).
- Nota: si arranca con varios workers puede dar un `DeadlockDetectedError`
  transitorio al crear tablas; un segundo reinicio arranca limpio.

## 7. Pasos típicos tras git pull
```
# frontend webmail
cd frontend && npm run build && cp -a dist/. ../www/webmail/
# frontend panel
cd ../admin-panel/frontend && npm run build
# reiniciar backends
systemctl restart maquita-webmail maquita-admin
# nginx: agregar las 4 rutas OnlyOffice si no las tenés, luego:
nginx -t && systemctl reload nginx
```

## 8. Divisor arrastrable lista ↔ lectura (solo código + rebuild frontend)
Se puede arrastrar el borde entre la lista de mensajes y el panel de lectura
para ajustar su ancho; el panel derecho se adapta solo. Ancho persistido en
localStorage (260-720px). Archivos: mailStore.ts (messageListWidth),
MessageList.tsx (style width), MailView.tsx (componente ListResizeHandle).
Con git pull + rebuild del frontend funciona directo.

## 9. IMPORTANTE: permisos de las tablas de config
Las tablas office_config / voice_config / ai_config las usa el webmail. Si las
crea otro usuario de PostgreSQL (p.ej. postgres o el del panel), el usuario del
webmail no podra leerlas y el dictado dara 503 / el wiring caera al .env. Tras
crearlas, da permisos al usuario del webmail (el de DATABASE_URL):
```
GRANT SELECT, INSERT, UPDATE, DELETE ON office_config TO <usuario_webmail>;
GRANT SELECT, INSERT, UPDATE, DELETE ON voice_config  TO <usuario_webmail>;
GRANT SELECT, INSERT, UPDATE, DELETE ON ai_config     TO <usuario_webmail>;
```

## 10. Fix tasks 500 (column tc.board_id does not exist)
_verify_card_access en tasks/router.py navegaba card->board con tc.board_id, pero
task_cards no tiene board_id (es card->list->board). Corregido con JOIN via
task_lists. Solo backend.

## 11. WhisperLive — dictado en vivo privado (3er modo)
Servicio de transcripcion en streaming (WebSocket) sobre GPU. En Maquita corre
en VM170 (Tesla P40). Para reproducirlo:
- Servidor: pip install whisper-live (sin PyAudio) en un venv; systemd que corra
  `whisper_live.server.TranscriptionServer().run(host=0.0.0.0, port=9090,
  backend=faster_whisper, single_model=True, rest_port=9091)` con
  LD_LIBRARY_PATH a las libs nvidia/cudnn|cublas del venv. GPU Pascal usa float32.
- Firewall del server: abrir el 9090 (en Maquita, nftables permite 9090 desde la LAN).
- nginx del webmail: ruta /whisperlive/ (WebSocket) -> IP:9090 del servidor (ya en
  la plantilla webmail.conf; ajustar IP).
- Panel :8443 -> Dictado por voz -> elegir "Whisper en vivo (privado)".
El frontend manda PCM float32 16kHz por WebSocket y recibe segments en vivo.
